# QA — 2026-08-22

## Автоматические контракты

- `python3 -m unittest -v tests/test_data.py tests/test_build.py` — 15 тестов, `OK`.
- `node tests/core_behavior.mjs` — `core behavior ok`.
- Ровно 26 табаков, 15 допустимых аналогов и 216 рецептов.
- Все рецепты используют 2–4 существующих компонента; доли кратны 5 и дают 100%.
- Внутренняя крепость каждого микса независимо пересчитана; «Очень крепкая» отсутствует.
- Sebero не попадает в крепкую категорию; Dogma и Kraken не занижены до средней.
- В видимом статическом тексте нет `/10` и числовой шкалы крепости.
- В сборке нет CDN, внешних scripts/styles, `fetch`, XHR или service worker.

## Браузерная проверка

- 1440×900, светлая тема: `desktop-1440.png`.
- 1440×900, тёмная тема: `desktop-1440-dark.png`.
- 390×844: `mobile-390.png`, overflow 0 px, минимальный видимый button 44 px.
- 375×844: `mobile-375.png`, overflow 0 px.
- Подробный рецепт: `mobile-recipe-detail.png`.
- Прямой запуск по `file://`: `file-scheme-1440.png`; HTML и встроенный JavaScript загрузились без сервера. Мобильная адаптивность отдельно проверена реальными CSS-viewports 390×844 и 375×844.
- Консоль: 0 errors, 0 warnings.
- Сетевой журнал: только сам локальный HTML; внешних ресурсных запросов нет.
- После отключения Sebero Strawberry: полка 25/26, выключенный ингредиент отсутствует во всех отображаемых карточках; после восстановления — 26/26.
- Избранное и «Пробовал» переживают reload через `localStorage`.
- После перевода загруженной страницы в offline режим направление меняется без ресурсных запросов.

## Дизайн-ревизия

- Единственный detector-pass работал в regex fallback и выдал advisory о координатной сетке. На этом продукте это осознанная журнально-измерительная поверхность, то есть исключение самого правила.
- Начальный verdict: `fix`; закрыты вырубленная геометрия CTA и единый SVG закрытия.
- Повторный verdict: `ship`. Подробности: `design-review-initial.md`, `design-review-verdict.md`.
- Ревизия выполнена в одном контексте, потому что правила сессии запрещали делегирование sub-agent.

## Внешняя приёмка

Физический iPhone недоступен автоматизированной среде. Последний owner-step: перенести `dist/xyka_mix_lab_2026-08-22.html` в «Файлы», открыть на реальном iPhone и подтвердить работу касаний и сохранения после повторного открытия.
