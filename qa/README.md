# QA — очистка каталога, 2026-08-23

## Автоматические контракты

- `python3 -m unittest -v tests.test_data tests.test_build` — 27 тестов, `OK`.
- `node tests/core_behavior.mjs` — `core behavior ok`.
- `node --check src/app.js` — синтаксис корректен.
- Строгий frontend premium audit — 0 findings; установленная upstream-версия skill совпадает с проверенной ревизией.
- `index.html` и `dist/xyka_mix_lab_2026-08-22.html` побитно идентичны.

Каталог содержит 101 рецепт в 20 непустых ячейках: 93 авторских, 7 адаптированных и 1 экспериментальный. В каждой ячейке 1–8 строк; 45 рецептов используют точные сектора, 11 — минимум два реальных слоя, 45 — короткий метод «компот».

Тесты независимо проверяют уникальность наборов, overlap `< 0,95` глобально и `< 0,85` внутри ячейки, `qualityScore ≥ 0,65`, честную долю крепких табаков, официальные бренд/вкус, 101 уникальное говорящее название, отсутствие recipe hook и публичной секции «Ноты», грамматику редакционных пояснений и полную геометрию укладки. Построчная матрица: `recipe-content-review.md`.

## Браузерная проверка

- 375×844, 390×844, 427×922 и 1440×900: горизонтальный overflow 0 px.
- Светлая и тёмная темы проверены; при `prefers-reduced-motion: reduce` переходы сведены к 0,01 ms.
- Минимальная высота видимой кнопки — 44 px в выбранном состоянии и 48 px на первом экране.
- Клавиатура: `Enter` выбирает направление; `Escape` закрывает drawer и возвращает фокус на карточку.
- После перехода загруженной страницы в offline режим направление меняется и выдача пересчитывается без сети.
- Консоль: 0 errors, 0 warnings. В финальном smoke сетевой журнал содержит только один локальный GET `index.html`; внешних запросов нет.
- Миграция v1→v2 переносит только `availableIds`; избранное, «Пробовал» и фильтры сбрасываются.
- Прямой запуск автономного `index.html` по `file://` подтверждён отдельным headless Chrome. Адаптивные размеры проверены Playwright по локальному HTTP, поскольку CLI блокирует протокол `file:` до запуска страницы.

## Визуальные доказательства

- `mixlab-390-initial.png` — первый экран 390 px.
- `mixlab-390-selection.png` и `mixlab-1440-selection.png` — автоматическая выдача без submit-кнопки.
- `mixlab-390-packing.png` — круглая секторная капсула: проценты внутри долей, локальные иконки снаружи на светлых подложках.
- `mixlab-390-layers.png` — разрез капсулы с двумя пропорциональными слоями и пояснением порядка закладки до переворота.
- `mixlab-427-dark.png` — тёмная тема и выбранное состояние.
- `file-scheme-390.png` — автономная загрузка по `file://`.

Физический iPhone недоступен автоматизированной среде; финальная owner-проверка касаний в Safari остаётся желательной перед публикацией.
